---
name: weekly-blog-draft
description: "결제·핀테크 엔지니어링 브리핑" 블로그의 주간 포스트 작성 루틴. 소재 선정 기준, 리서치 규칙, 작성 구조, 발행 규칙을 담은 살아있는 운영 매뉴얼 — 매 실행마다 먼저 읽고 따르며, 실행 중 배운 것을 이 문서에 반영해 계속 갱신한다.
---

# 주간 블로그 포스트 작성 루틴

이 문서는 이 저장소의 주간 포스트 자동 작성 루틴(GitHub Actions `weekly-post-draft.yml`, 또는 사용자가 직접 요청하는 추가 작성 작업)이 매번 참고하는 단일 진실 소스다. 프롬프트에 내용을 중복해서 박아넣지 말고, 이 문서를 갱신해서 다음 실행이 자동으로 최신 판단 기준을 물려받게 한다.

## 블로그 컨셉

- 이름: 결제·핀테크 엔지니어링 브리핑
- PG/간편결제/전자금융 규제·업계 동향과, PG·선불 사업 운영에 필요한 인프라·시스템 구성을 엔지니어 관점에서 분석
- 운영자는 익명이다 — 소속·직함·실명 등 신상정보를 글 본문, 커밋 메시지, PR 본문 어디에도 절대 포함하지 않는다
- 차별화 포인트는 "양산형 AI 콘텐츠"가 아니라는 것. 반드시 검증 가능한 구체적 근거(실제 뉴스/공시, 또는 실제 서비스명·버전·설정값)를 갖춰야 한다
- 독자는 전문가가 아니다. "이 규제가 나한테 무슨 뜻이지", "이거 어떻게 대응해야 하지" 하고 막연히 검색해서 들어오는 사람이 기본 페르소나다 — 시니어 개발자만 겨냥하지 않는다. 그렇다고 유치하게 쓰라는 뜻은 아니다. 내용의 깊이·정확성은 유지하되, 배경지식을 전제하지 않고 처음 보는 사람도 따라올 수 있게 쓴다. 자세한 규칙은 "쉬운 글쓰기 규칙" 섹션 참고

## 쉬운 글쓰기 규칙 (중요 — 모든 글에 적용)

2026-09-01에 사용자가 직접 지적한 문제: 기존 글들이 "시니어 개발자 전제 + 기초 설명 생략"으로 쓰여서, 실제 독자(전문가라기보다 "이거 어떻게 해야 하지" 하고 검색해 들어온 사람)에게는 진입장벽이 너무 높았다. 앞으로는 이 규칙을 기본값으로 따른다.

- **처음 나오는 용어·조문·서비스명은 그 자리에서 짧게 풀어준다.** "전자금융감독규정 제14조의2에 따라" 대신 "전자금융감독규정(금융회사가 지켜야 하는 보안·운영 규칙을 정한 규정)의 클라우드 이용 관련 조항에 따라"처럼, 처음 보는 사람이 문장만 읽고도 뭔지 감이 오게 쓴다. 이미 이 글 안에서 한 번 풀어준 용어는 이후엔 줄여 써도 된다
- **"왜 이게 나한테 중요한지"를 각 섹션 첫 문장에 놓는다.** 조문 요약부터 시작하지 않고, "이거 안 하면 어떤 일이 생기는지"부터 말한 다음 세부 내용으로 들어간다
- **문장은 짧게, 하나의 문장에 하나의 생각만.** 여러 조건과 예외를 한 문장에 욱여넣지 않는다
- **숫자·기준은 비교 대상을 붙여서 감을 준다.** "RTO 3시간"이라고만 하지 않고 "장애가 나면 3시간 안에 복구해야 한다는 뜻"까지 붙인다
- **정확성은 절대 희생하지 않는다.** 쉽게 쓰는 것과 틀리게 쓰는 것은 다르다 — 단순화하다가 규정 내용이 왜곡되면 안 된다. 어려운 개념을 없애는 게 아니라, 그 개념까지 가는 계단을 하나씩 만들어주는 것이다
- **체크리스트·참고자료는 그대로 실무형으로 유지한다.** 쉬운 글쓰기는 설명 방식에 적용하는 규칙이고, 체크리스트 항목 자체를 유치하게 바꾸라는 뜻이 아니다
- 어린아이에게 말하듯 반말·과장된 비유를 쓰라는 뜻이 아니다("~하는 거예요~" 같은 말투 금지). 성인 대상의 명료한 설명체를 유지한다

## 내부 링크 규칙 ("관련 글" 섹션, 2026-09-01 신설)

모든 글은 "참고자료" 섹션 다음, 글의 맨 마지막에 "## 관련 글" 섹션을 둔다. 이걸 빠뜨리면 안 된다.

- `content/posts/`의 기존 글들을 훑어서, 주제·규제·시스템이 겹치는 글이 있으면 링크한다 (제목 그대로 링크 텍스트로 쓰고, 뒤에 " — 왜 관련 있는지" 한 줄을 붙인다)
- 링크는 2~3개가 적당하다. 억지로 채우지 말고, 진짜 관련 있는 것만 넣는다 — 관련 있는 글이 하나도 없으면 섹션 자체를 생략해도 된다
- 이 링크는 본문 중간의 문맥적 인라인 링크(예: "[지난 글](...)에서 다룬")와는 별개다. 인라인 링크는 계속 써도 되지만, 그것과 별개로 글 끝에 모아서 훑어볼 수 있는 목록을 남긴다
- 새 글을 쓴 뒤에는, 관련된 기존 글에도 이번 새 글로 가는 링크를 추가할지 검토한다(양방향 연결). 특히 새 글이 기존 글의 후속편이거나 같은 규제 세트의 일부라면 기존 글도 갱신한다

## 시각 요소 규칙 (표·도식, 2026-09-01 신설)

2026-09-01에 사용자가 "글이 텍스트만 있어서 심심하다, 표·이미지·도식화를 중간중간 넣으라"고 요청했다. 앞으로 모든 글은 다음을 따른다.

- **표(마크다운 테이블)를 최소 1개 이상 넣는다.** 옵션을 비교하거나(예: 오픈소스 A vs B vs C), 개정 전/후를 비교하거나, 단계별 일정(예: 연도별 기준 확대)을 보여줄 때는 문장으로 풀어쓰지 말고 표로 정리한다. `lib/posts.ts`의 마크다운 처리 파이프라인은 `remark-gfm`을 포함하므로 표준 GFM 표 문법(`| 헤더 | 헤더 |` 형식)을 그대로 쓰면 된다
- **절차·구조를 설명할 때는 도식을 고려한다.** 여러 단계를 거치는 절차(예: 클라우드 이용 7단계, 정산 배치 상태 머신)나 여러 주체 사이의 관계(예: 1차 PG-2차 PG-가맹점 구조)는 텍스트로 나열하는 대신 간단한 다이어그램으로 보여주는 걸 우선 검토한다. 이 저장소는 이미지 생성 도구가 따로 없으므로, 직접 그린 SVG 파일을 `public/images/`에 저장하고 마크다운 이미지 문법(`![대체 텍스트](/images/파일명.svg)`)으로 삽입한다. SVG는 사이트 다크 테마 색상(`--bg`, `--fg`, `--muted`, `--accent`, `--border` — `app/globals.css` 참고)에 맞춰 그린다
- 표·도식 모두 "예쁘게 만들기 위한 장식"이 아니라 "글로 설명하면 오히려 복잡한 내용을 정리해주는 도구"로 쓴다. 억지로 넣지 말고, 실제로 비교·절차·구조가 있는 곳에 쓴다

## 콘텐츠 트랙

- **트랙 A(규제·정책, 우선순위 높음)**: 그 주 실제 뉴스/공시 기반. 신뢰 출처 — 금융위원회(fsc.go.kr), 금융감독원, 법제처/국가법령정보센터(moleg.go.kr, law.go.kr), 전자신문, 머니투데이, 로펌 뉴스레터(김앤장, 율촌 등), KDI 경제정보센터
- **트랙 B(인프라·시스템, 트랙 A에 마땅한 소재가 없는 주)**: AWS/클라우드 인프라 설계, 전자금융감독규정상 클라우드 이용 신고 등 컴플라이언스, PG/선불 사업에 필요한 시스템 구성, 오픈소스·상용 툴 비교. 신뢰 출처 — AWS 공식 문서/블로그(docs.aws.amazon.com, aws.amazon.com/blogs), 오픈소스 프로젝트 공식 저장소/릴리스노트, 전자금융감독규정 원문, 금융보안원(fsec.or.kr) 가이드

트랙 B 글도 일반론으로 채우면 안 된다 — 실제 조문 번호, 실제 AWS 서비스명·설정, 실제 오픈소스 프로젝트명·버전까지 구체적으로 검증한다.

## 실행 순서

1. `content/posts/`의 기존 파일명·frontmatter를 확인해 이미 다룬 주제를 파악한다 (중복 방지)
2. `README.md`의 "콘텐츠 캘린더", "SEO 키워드 전략" 섹션을 읽는다
3. 소재 선택 순서: ① 트랙 A 리서치를 먼저 시도한다 — 구체적이고 시의성 있는 소재(특히 시행일 임박, 최근 개정)가 있으면 그것으로 진행. ② 없으면 README "전자금융업 등록 시리즈"에서 아직 안 쓴 편을 하나 고른다. 이 시리즈는 뉴스 기반이 아니라 "등록 어떻게 하는지" 검색해 들어오는 사람을 위한 평가 참고형 콘텐츠이고, **한 실행에서 한 편만** 쓴다 — 여러 편을 몰아 쓰지 않는다. ③ 시리즈도 이미 다 썼거나 이번 주 소재로 안 맞으면 트랙 B로 넘어간다
4. WebSearch/WebFetch로 1차 출처(법령 원문, 보도자료 원문, 공식 문서)까지 확인한다 — 2차 가공 기사 하나만으로 근거를 삼지 않는다
5. `content/posts/2026-09-01-pg-settlement-fund-segregation.md`의 구조를 따라 작성한다: 배경 / (트랙 A: 핵심 변경 내용, 트랙 B: 아키텍처 옵션 비교·트레이드오프) / 실무 체크리스트 / "참고자료" / "관련 글". 문장 톤은 "쉬운 글쓰기 규칙"을 따르고, "관련 글" 섹션은 아래 "내부 링크 규칙"을 따른다

## Frontmatter 규격

```yaml
title: "..."
description: "..."
date: "YYYY-MM-DD"
tags: ["...", "...", "...", "..."]
keywords: ["...", "...", "...", "...", "..."]
category: "regulation" # "regulation" | "infra" | "guide" — 아래 판단 기준 참고
```

**`category`는 트랙(어디서 소재를 찾았는지)이 아니라 독자의 검색 의도로 정한다.** 트랙 A에서 찾은 소재라고 자동으로 `regulation`은 아니고, 트랙 B에서 찾았다고 자동으로 `infra`도 아니다 — 실제로 어떻게 판단하는지는 다음을 본다.

- **`regulation`**: "이번 주에 뭐가 바뀌었지"에 답하는 시의성 있는 규제·정책 뉴스. 시행일, 최근 개정처럼 "지금 봐야 하는" 내용
- **`infra`**: "이럴 때 뭘 골라야 하지"에 답하는 아키텍처·도구 선택 트레이드오프. 여러 옵션을 놓고 비교하는 내용(오픈소스 vs 상용, 이 방식 vs 저 방식)
- **`guide`**: "이거 어떻게 하는 거지"에 답하는 절차형 참고 가이드. 등록·신고·허가처럼 정해진 절차를 밟아야 하는 내용이면, 그 절차에 AWS 서비스 설정 같은 기술적 세부사항이 섞여 있어도(예: 클라우드 이용 신고 절차 글) `guide`다 — 소재를 트랙 B(인프라)에서 찾았어도 마찬가지다

애매하면 "이 글을 검색해서 들어온 사람이 원하는 게 오늘의 뉴스인지, 여러 선택지 비교인지, 절차 안내인지"로 판단한다. `category`는 홈페이지 카테고리 필터(`components/PostList.tsx`, 값 정의는 `lib/categories.ts`)가 그대로 쓰는 값이니 잘못 넣거나 비워두면 엉뚱한 탭에 잡히거나 아예 안 잡힌다.

`keywords`는 README "SEO 키워드 전략" 목록과 연계해서 채운다. 새로운 롱테일 키워드를 발견하면 README에도 추가한다.

## 날짜 지정 규칙 (재발 이슈 — 반드시 지킬 것)

`lib/posts.ts`는 `date` 필드 내림차순으로 정렬하는데, 날짜가 같으면 정렬 순서가 파일시스템 `readdir` 순서에 의존해 보장되지 않는다.

- 한 번의 실행에서 글을 **하나만** 낼 때는 실제 오늘 날짜를 쓰면 된다 (정상 케이스, 충돌 없음)
- 여러 편을 한 번에 등록할 때는(백필, 배치 작성 등) 서로 다른 날짜를 부여해서 항상 "가장 최근에 다룬 주제가 목록 최상단"이라는 사용자 기대를 지킨다. 미래 날짜를 붙여 "예약 발행"처럼 보이게 하지 말고, 과거 빈 주차를 채우는 방식으로 날짜를 분산한다
- **시리즈물(등록1→2→3처럼 순서가 있는 글)을 여러 편 한 번에 쓸 때**: 날짜가 꼭 일주일 단위로 떨어져 있어야 하는 건 아니다. 이미 발행된 시리즈 1편(예: 07-21) 뒤에 이어지는 편들을 쓴다면, 그 다음 기존 글(예: 07-28) 사이의 빈 며칠(07-22, 07-24, 07-26)에 순서대로 배치해서 "시리즈 번호 = 날짜 순서"를 유지한다. 시리즈 1편보다 앞선 날짜에 2편을 넣으면(날짜 순서상 2편이 1편보다 먼저 보임) "다음 편에서 다룬다" 같은 앞글의 예고 문장과 실제 날짜 순서가 어긋난다

## 발행 규칙

- **main에 직접 커밋/푸시하지 않는다.** 항상 새 브랜치 → PR을 먼저 연다.
- **PR을 연 뒤, `npm run build` 검증을 통과했으면 그대로 main에 머지까지 진행한다** (2026-09-02, 사용자 지침으로 변경). 이전에는 "머지는 사람이 한다"였으나, 사용자가 직접 "머지까지 해, 이건 지침으로 해"라고 명시적으로 요청해 자동 머지로 정책을 바꿨다. draft PR은 머지 전에 ready for review로 전환한다. 빌드가 실패하거나 병합 충돌이 있으면 머지하지 말고 상태를 남긴다.
- PR 제목/본문에도 신상정보를 넣지 않는다
- 빌드 검증(`npm run build`) 없이 PR을 열지 않는다. 검증 후 `next-env.d.ts`, `package-lock.json` 같은 `npm install`/`build`의 부수 변경은 커밋에서 제외한다

## SEO 진화 규칙

콘텐츠 신선도만으로는 구글 노출이 늘지 않는다. 매 실행마다(또는 콘텐츠 작업이 없는 주라도 요청받으면) 아래를 함께 점검하고, 부족하면 코드/문서를 고친다.

- **내부 링크**: 새 글 본문에 관련 기존 글을 최소 1개 이상 링크한다 (`/posts/<slug>` 형태). 관련 글이 없으면 스킵 가능하지만, 있는데 안 걸면 감점 요인이다.
- **메타 설명 길이**: frontmatter `description`은 120~160자 권장(구글 스니펫이 잘리지 않는 범위). 새 글 작성 시 확인한다.
- **구조화 데이터**: `app/posts/[slug]/page.tsx`가 BlogPosting JSON-LD를 렌더링한다 (2026-09-01 추가). 페이지 구조를 바꿀 때 이 태그가 깨지지 않는지 확인한다.
- **canonical/OG/Twitter 메타**: `app/layout.tsx`, `app/posts/[slug]/page.tsx`에 canonical·OpenGraph·Twitter Card가 설정돼 있다 (2026-09-01 추가). 새 메타데이터 필드를 추가할 때 이 기준을 유지한다.
- **키워드 전략 갱신**: 이번 글에서 새로 검증한 롱테일 키워드가 있으면 README "SEO 키워드 전략"에 추가한다. 검색량을 직접 확인할 수단이 없으므로, 실무자가 실제로 검색할 법한 구체적 문구(제품명·법조문번호·에러코드 등)를 우선한다 — 뭉뚱그린 키워드는 추가하지 않는다.
- **og:image**: 2026-09-03에 해결됨. `app/opengraph-image.tsx`(홈페이지용 정적 이미지)와 `app/posts/[slug]/opengraph-image.tsx`(글마다 제목·카테고리·날짜를 렌더링하는 동적 이미지)를 추가했다. `next/og`의 `ImageResponse`(Satori)는 한글 폰트를 기본 내장하지 않고 woff2도 지원하지 않으므로, `lib/og-font.ts`가 구형 브라우저 User-Agent로 위장해 Google Fonts CSS API에서 "필요한 글자만" 담긴 Noto Sans KR woff 서브셋을 받아온다 — 이 트릭 없이 시도하면 한글이 빈 박스로 렌더링된다. 새 글을 추가할 때 별도 작업은 필요 없다(제목·카테고리·날짜를 자동으로 읽어 렌더링).

## 신뢰 출처 목록 (누적)

- 금융위원회 — fsc.go.kr
- 법제처/국가법령정보센터 — moleg.go.kr, law.go.kr
- 전자신문, 머니투데이
- 법무법인 뉴스레터 — 김앤장, 율촌
- KDI 경제정보센터 — eiec.kdi.re.kr
- AWS 공식 문서/블로그 — docs.aws.amazon.com, aws.amazon.com/blogs
- 금융보안원 — fsec.or.kr
- 뉴스핌, 헤럴드경제 — 금융위 정례회의 의결 사항을 법무법인 뉴스레터보다 먼저 속보로 다루는 경우가 많음

## 운영 중 발견한 이슈 (변경 이력)

- 2026-09-01: 여러 글을 같은 날짜로 한꺼번에 등록하면 목록 정렬이 동률로 보장되지 않음을 발견 → "날짜 지정 규칙" 추가, 기존 글 2편을 실제 발행 순서에 맞게 백데이트
- 2026-09-01: 콘텐츠 범위를 트랙 A(규제·정책) 단일에서 트랙 B(인프라·시스템 구성) 추가로 확장
- 2026-09-01: 이 SKILL.md 신설 — 기존에 `.github/workflows/weekly-post-draft.yml` 프롬프트에 인라인으로 박아두던 지침을 이 문서로 이전. 워크플로우 프롬프트는 이제 이 문서를 읽으라고 지시하는 짧은 래퍼 역할만 한다
- 2026-09-01: 구글 노출 개선을 위해 canonical 태그, OpenGraph/Twitter 메타, BlogPosting JSON-LD 구조화 데이터를 코드에 추가. "SEO 진화 규칙" 섹션 신설 — 콘텐츠만으로는 부족하고 기술적 SEO 요소도 매 실행마다 점검·개선 대상임을 명시
- 2026-09-01: 트랙 B 글 3편을 한 번에 등록한 실제 사례 — "날짜 지정 규칙"에 따라 오늘 날짜 대신 과거 빈 주차(07-28, 08-04, 08-11)를 채우는 방식으로 날짜를 분산했고, 관련 기존 글로 상호 내부 링크를 걸었다
- 2026-09-01: 홈페이지에 카테고리 필터(전체/규제·정책/인프라·시스템) UI 추가. frontmatter에 `category` 필드가 신설됐으므로 새 글 작성 시 반드시 트랙과 일치하는 값을 채운다
- 2026-09-01: 사용자가 "글이 전문가 전제로 너무 어렵다"고 지적 → 독자 정의를 "시니어 개발자"에서 "막연히 검색해 들어온 비전문가"로 바꾸고 "쉬운 글쓰기 규칙" 섹션 신설. 기존 6편도 이 규칙에 맞춰 다시 썼다 — 다음 글부터는 처음부터 이 규칙으로 쓸 것
- 2026-09-01: 사용자가 "전자금융업 등록 절차/요건"도 다루면 좋겠다고 요청하되 한 번에 몰아 쓰지 말고 실행마다 나눠 쓰라고 명시 → README에 "등록 시리즈" 후보 5편을 큐로 추가하고, 소재 선택 순서에 트랙 A 다음 우선순위로 끼워 넣었다
- 2026-09-01: 등록 시리즈 1편(전자금융업 종류·등록 대상 판단) 발행. 여러 편을 한 실행에서 몰아 쓴 게 아니라 사용자가 "지금 1편만 써두자"고 명시적으로 요청한 경우였다 — 시리즈 규칙(한 실행에 한 편)은 유지하되, 사용자가 명시적으로 여러 편을 요청하면 그 지시가 우선한다. 날짜는 기존 글과 안 겹치게 07-21(07-28보다 한 주 이전)로 배치. 다음은 등록2(PG업 등록 요건)부터
- 2026-09-01: 사용자 제안으로 등록 시리즈를 별도 카테고리 `guide`로 분리 (`lib/categories.ts`에 세 번째 값 추가). 앞으로 등록 시리즈 글은 `category: "guide"`로 쓴다 — `regulation` 아님
- 2026-09-01: 사용자가 "클라우드 이용 신고" 글(트랙 B에서 작성)도 `guide`가 맞다고 지적 → `category`를 트랙 기준 기계적 매핑이 아니라 독자 검색 의도 기준으로 판단하도록 규칙을 다시 씀. 절차형 콘텐츠는 소재를 어느 트랙에서 찾았든 `guide`
- 2026-09-01: 구글 애드센스 신청을 위해 콘텐츠 개수를 채워달라는 요청으로 등록 시리즈 2~4편(자본금·인력 요건, 선불업 등록·면제, 신청 절차·서류)을 한 실행에서 몰아 씀 — "사용자가 명시적으로 여러 편을 요청하면 그 지시가 우선한다" 규칙 재확인. 이때 날짜를 07-21(1편) 뒤 07-22/07-24/07-26으로 배치해 시리즈 번호와 날짜 순서를 맞춤(위 "날짜 지정 규칙" 참고)
- 2026-09-01: 애드센스 사이트 확인 스크립트(`client=ca-pub-...`)를 `app/layout.tsx`에 `next/script`로 추가. 실제 광고 슬롯(`ins.adsbygoogle`)은 아직 아니며, `components/AdSlot.tsx`는 계속 플레이스홀더 — 심사 승인 후 광고 단위 코드를 받으면 교체 필요
- 2026-09-02: 같은 실행에서 글 한 편만 쓰는데도 "오늘 날짜"(시스템이 알려주는 currentDate)가 이미 다른 실행에서 쓰인 날짜와 겹치는 경우를 만남(기존에 2026-09-01로 발행된 글이 3편). "날짜 지정 규칙"의 단일 편 케이스는 "충돌 없음"을 전제하는데, 이 전제가 깨지면 정렬 보장이 다시 깨진다 → 그 경우 배치 등록처럼 날짜를 인위적으로 분산하기보다, 기존 최신 날짜 다음 날짜 하나를 그대로 쓰는 게 가장 단순하고 "최신 글이 맨 위" 기대와도 맞는다(예: 기존 최신이 09-01이면 새 글은 09-02). "미래 날짜로 예약 발행처럼 보이지 말 것" 규칙은 실제 며칠~몇 주씩 앞서가는 날짜에 적용되는 것이지, 겹친 날짜를 하루 미루는 것까지 막는 취지는 아니다
- 2026-09-02: 트랙 A 리서치 중 금융위원회 정례회의 의결 사항(경영지도기준 위반 전자금융업자 10곳 자본금 증액 명령, 2026-07-31)을 소재로 채택. 이 사례처럼 "그 주 헤드라인 뉴스"뿐 아니라 금융위 정례회의 의결 사항·조치 목록도 매주 확인할 가치가 있는 소스임을 확인 — 검색어에 "금융위원회 정례회의 전자금융업자 조치"류를 추가로 시도해볼 것
- 2026-09-02: 사용자가 "백엔드"라는 지칭이 부적절하다고 지적 → 글을 실제로 읽는 대상(엔지니어이긴 하지만 "백엔드"로 층까지 좁힐 이유는 없다)에 맞춰 "개발팀"(구체적 실행 주체를 가리킬 때), "엔지니어"·"시스템"(분석 관점·아키텍처를 가리킬 때)으로 전체 교체. 기존 발행 글 전체(제목·소제목·설명·상호링크 텍스트), README, 이 SKILL.md, `app/layout.tsx`, `app/page.tsx`의 사이트 소개 문구까지 모두 고쳤다. **앞으로 새 글을 쓸 때도 "백엔드"라는 표현 자체를 쓰지 않는다** — "OO가 백엔드에 요구하는 것" 같은 제목·섹션 패턴 대신 "OO가 개발팀에 요구하는 것"(실행 주체를 가리킬 때) 또는 "OO가 시스템에 요구하는 것"(아키텍처·구조를 가리킬 때)을 쓴다. 글 하나를 고치면서 상호링크로 그 제목을 인용하는 다른 글들도 같이 찾아 고쳐야 한다는 걸 다시 확인 — 제목을 바꿀 때는 항상 `grep`으로 그 제목 문자열을 인용하는 다른 파일이 있는지부터 확인할 것
- 2026-09-03: "날짜 지정 규칙"의 충돌 회피(최신 날짜 다음 날짜 사용)가 두 번 연속으로 발생한 사례 — 기존 최신이 09-02였던 상태에서 또 한 번 겹쳐 09-03을 썼다. 하루씩 계속 밀리는 방식이라도 "최신 글이 맨 위" 기대를 지키는 한 문제없다는 걸 재확인. 소재는 금융위 2026-07-31 정례회의 의결 사항 중 이미 다른 글(경영개선명령)이 다룬 것과 별개인 「금융복합기업집단 감독규정」 개정(전자금융업자의 소속금융회사 편입)을 채택 — 같은 정례회의 하나에서 서로 다른 글감이 여러 개 나올 수 있다는 사례. 리서치 중 중요했던 점: 이 규제는 "모든 PG·선불사"가 아니라 "이미 금융복합기업집단으로 지정된 대형 그룹(자산 5조원 이상 + 여신/금투/보험 중 2개 이상 영위)에 속한 전자금융업자"에만 적용된다 — 이런 지정 요건·적용 범위를 확인하지 않고 쓰면 실제로는 해당 없는 다수 독자에게 잘못된 경보를 주게 되므로, 새 규제를 다룰 때는 항상 "이 규제가 실제로 누구에게 적용되는지"부터 1차 출처로 확인할 것
- 2026-09-03: 사용자가 "유입 확대"를 요청해 og:image(공유 미리보기 이미지) 부재 문제를 해결. 콘텐츠 작업이 아니라 기술 SEO/공유 최적화 요청이었다는 점에서, 이 스킬이 콘텐츠 발행뿐 아니라 이런 요청에도 이어질 수 있음을 확인 — 위 "og:image" 항목 참고. 구현 중 겪은 문제와 해결책: (1) Satori(next/og)는 한글 글리프가 없는 폰트를 기본 사용해 아무 대비 없이 쓰면 한글이 빈 박스로 나온다 → Google Fonts CSS API를 구형 브라우저 UA로 호출해 필요한 글자만 담은 woff 서브셋을 받아와야 한다(woff2는 Satori가 못 읽는다). (2) 동적 라우트(`app/posts/[slug]/opengraph-image.tsx`)는 페이지의 `generateStaticParams`와 별개로 이미지 파일 자체에도 `generateStaticParams`를 따로 export해야 각 글마다 빌드타임에 정적 생성된다. (3) 글 제목 길이 편차가 커서(31~56자) 고정 폰트 크기로는 안 맞는 제목이 나온다 → 글자 수 구간별로 폰트 크기를 다르게 주는 함수를 넣었다(`titleFontSize`). 앞으로 og:image 관련 코드를 만질 때는 `npm run build` 후 `.next/server/app/**/opengraph-image.body`를 실제로 열어 한글이 제대로 렌더링됐는지 눈으로 확인할 것 — 타입체크·빌드 통과만으로는 폰트 깨짐을 못 잡는다

## 이 문서 갱신 규칙 (자기 진화)

이 스킬을 따르기만 하지 말고, 실행 중 발견한 새로운 판단 기준·재발 방지 규칙·유용한 출처가 있으면 이 문서에 함께 반영해서 같은 커밋/PR에 포함시킨다:

- 실수를 발견해 고쳤다면 "운영 중 발견한 이슈" 섹션에 원인과 해결책을 한두 줄로 추가한다
- 새로운 신뢰 출처를 찾았다면 "신뢰 출처 목록"에 추가한다
- 트랙 선택, 날짜 지정, 구조 등 반복되는 판단에 대해 더 나은 규칙을 찾았으면 해당 섹션을 직접 고친다 (이력에만 남기고 규칙 본문은 안 고치면 다음 실행이 못 물려받는다)

이 문서는 코드와 함께 커밋되는 살아있는 매뉴얼이다. 매 실행이 다음 실행을 위한 지식을 남겨야 한다.
